[package]
name = "fb"
version = "0.1.0"
edition = "2024"

[features]
default = []
debug-lock = []  # Emit serial warnings on lock contention in ISR context

[dependencies]
spin = "0.9"
boot-proto = { path = "../../boot/boot-proto" }
