[package]
name = "mm-swap"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Swap subsystem — swap areas, page-out/page-in, swap cache, swappiness"

[dependencies]
os_core = { workspace = true }
mm-pagedb = { workspace = true }
mm-vmstat = { path = "../mm-vmstat" }
mm-traits = { workspace = true }
spin = { workspace = true }
os_log = { path = "../../core/os_log" }
