[package]
name = "doom"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
libc = { path = "../../libs/libc" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
