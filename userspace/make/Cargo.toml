[package]
name = "oxide-make"
version = "0.1.0"
edition = "2024"
description = "OXIDE OS make - minimal build automation tool"

[[bin]]
name = "make"
path = "src/main.rs"

[dependencies]
libc = { path = "../libc" }
