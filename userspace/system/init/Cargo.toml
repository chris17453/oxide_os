[package]
name = "init"
version = "0.1.0"
edition = "2024"
description = "Init process (PID 1) for OXIDE OS"

[[bin]]
name = "init"
path = "src/main.rs"

[dependencies]
libc = { path = "../../libs/libc" }
