[package]
name = "ssh"
version = "0.2.0"
edition = "2024"
description = "SSH client for OXIDE OS using oxide-std"

[[bin]]
name = "ssh"
path = "src/main.rs"

[dependencies]
oxide-std = { path = "../../libs/oxide-std" }
libc = { path = "../../libs/libc" }
