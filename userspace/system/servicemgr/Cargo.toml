[package]
name = "service"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Service manager daemon for OXIDE OS"

[dependencies]
libc = { path = "../../libs/libc" }
