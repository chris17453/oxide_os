[package]
name = "libc"
version = "0.1.0"
edition = "2024"
description = "Custom libc for OXIDE OS userspace"

[features]
debug-readline = []

[dependencies]
spin = { version = "0.9", default-features = false, features = ["mutex", "spin_mutex"] }

[lib]
crate-type = ["rlib", "staticlib"]
