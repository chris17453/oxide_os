[package]
name = "virtio-input"
version = "0.1.0"
edition = "2024"

[features]
default = []
debug-input = []  # Enable VirtIO input event tracing

[dependencies]
input = { path = "../../../input/input" }
pci = { path = "../../pci" }
driver-core = { path = "../../driver-core" }
virtio-core = { path = "../../virtio-core" }
mm-manager = { path = "../../../mm/mm-manager" }
mm-traits = { path = "../../../mm/mm-traits" }
spin = "0.9"
os_log = { path = "../../../core/os_log" }
os_core = { path = "../../../core/os_core" }
