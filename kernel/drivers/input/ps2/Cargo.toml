[package]
name = "ps2"
version = "0.1.0"
edition = "2024"

[features]
debug-input = []

[dependencies]
input = { path = "../../../input/input" }
# TODO: Add driver-core once circular dependency is resolved
# (ps2 → driver-core → pci → arch-x86_64 → ps2)
spin = "0.9"
