[package]
name = "virtio-emu"
version = "0.1.0"
edition = "2024"
description = "virtio device emulation for Oxide OS hypervisor"
license = "MIT OR Apache-2.0"

[dependencies]
spin = "0.9"
vmm = { path = "../vmm" }

[features]
default = []
