[package]
name = "mmap"
version = "0.1.0"
edition = "2021"
authors = ["OXIDE Contributors"]
description = "Memory mapping (mmap) implementation for OXIDE"
license = "MIT OR Apache-2.0"

[dependencies]
spin = "0.9"

[features]
default = []
