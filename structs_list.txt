if --enable-ipv6 is specified
if PTHREAD_SCOPE_SYSTEM is supported
if sockaddr has sa_len member
struct stat.st_birthtime
struct stat.st_flags
struct stat.st_gen
tv_nsec2 in struct stat
whether OpenSSL provides required hashlib module APIs
whether OpenSSL provides required ssl module APIs
whether RTLD_MEMBER is declared
whether WINDOW has _flags
whether _XOPEN_SOURCE should be defined
whether byte ordering is bigendian
whether compiling and linking against OpenSSL works
whether float word ordering is bigendian
whether mvwdelch is an expression
whether oxide-c++ also accepts flags for thread support
whether oxide-cc -fno-strict-aliasing accepts and needs -fno-strict-aliasing
whether oxide-cc accepts -Kpthread
whether oxide-cc accepts -Kthread
whether pthreads are available without options
whether to enable large file support
whether wchar_t is usable
whether we can use gcc inline assembler to get and set mc68881 fpcr