[package]
name = "driver-traits"
version.workspace = true
edition.workspace = true
license.workspace = true

# — SableWire: ZERO dependencies. That's the whole point of this crate.
# Breaking circular deps means this crate trusts nobody.
[dependencies]
