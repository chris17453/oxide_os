[package]
name = "rdp-proto"
version = "0.1.0"
edition = "2024"
authors = ["OXIDE Contributors"]
description = "RDP protocol parsing for OXIDE OS"
license = "MIT"

[dependencies]
rdp-traits = { path = "../rdp-traits" }

[features]
default = []
std = []
