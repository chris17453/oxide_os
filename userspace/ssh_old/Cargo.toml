[package]
name = "ssh_old"
version = "0.1.0"
edition = "2024"
description = "SSH client for OXIDE OS (old implementation)"

[[bin]]
name = "ssh_old"
path = "src/main.rs"

[dependencies]
libc = { path = "../libc" }
