[package]
name = "efflux-vmx"
version = "0.1.0"
edition = "2024"
description = "Intel VT-x (VMX) support for Efflux OS hypervisor"
license = "MIT OR Apache-2.0"

[dependencies]
spin = "0.9"
efflux-vmm = { path = "../efflux-vmm" }

[features]
default = []
