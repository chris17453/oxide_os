[package]
name = "sched-traits"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Scheduler trait definitions for OXIDE OS"

[dependencies]
os_core = { workspace = true }
