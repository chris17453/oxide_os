[package]
name = "tcpip"
version = "0.1.0"
edition = "2024"

[features]
debug-net = []

[dependencies]
dhcp = { path = "../dhcp" }
net = { path = "../net" }
os_log = { path = "../../core/os_log" }
spin = { workspace = true }
