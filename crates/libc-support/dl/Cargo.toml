[package]
name = "dl"
version = "0.1.0"
edition = "2021"
authors = ["EFFLUX Contributors"]
description = "Dynamic linker/loader (dlopen/dlsym) for EFFLUX"
license = "MIT OR Apache-2.0"

[dependencies]
spin = "0.9"

[features]
default = []
