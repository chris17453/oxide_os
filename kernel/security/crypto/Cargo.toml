[package]
name = "crypto"
version = "0.1.0"
edition = "2021"
authors = ["OXIDE Contributors"]
description = "Cryptographic primitives for OXIDE OS"
license = "MIT OR Apache-2.0"

[dependencies]
spin = { workspace = true }

[features]
default = []
std = []
