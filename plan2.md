## Phase 3 Gap Fill Plan (configure-driven)

Purpose: turn every failed probe from `plan.md` into actionable work items with the subsystem and file roots that should carry the fixes. Nothing below writes code yet—it is a backlog for kernel (`crates/*`, `kernel/src/*`), libc (`userspace/libc`), and the `external/cpython` port.

### Toolchain, configure flags, and ABI glue (`build/`, `toolchain/`, `external/cpython-oxide-config.site`)
- [ ] Wire up or intentionally gate `--enable-bolt`.
- [ ] Wire up or intentionally gate `--enable-framework`.
- [ ] Wire up or intentionally gate `--enable-loadable-sqlite-extensions`.
- [ ] Wire up or intentionally gate `--enable-optimizations`.
- [ ] Wire up or intentionally gate `--enable-profiling`.
- [ ] Wire up or intentionally gate `--enable-pystats`.
- [ ] Wire up or intentionally gate `--enable-shared`.
- [ ] Wire up or intentionally gate `--enable-wasm-dynamic-linking`.
- [ ] Wire up or intentionally gate `--enable-wasm-pthreads`.
- [ ] Wire up or intentionally gate `--with-address-sanitizer`.
- [ ] Wire up or intentionally gate `--with-assertions`.
- [ ] Wire up or intentionally gate `--with-computed-gotos`.
- [ ] Wire up or intentionally gate `--with-dsymutil`.
- [ ] Wire up or intentionally gate `--with-dtrace`.
- [ ] Wire up or intentionally gate `--with-libs`.
- [ ] Wire up or intentionally gate `--with-lto`.
- [ ] Wire up or intentionally gate `--with-memory-sanitizer`.
- [ ] Wire up or intentionally gate `--with-platlibdir`.
- [ ] Wire up or intentionally gate `--with-pydebug`.
- [ ] Wire up or intentionally gate `--with-pymalloc`.
- [ ] Wire up or intentionally gate `--with-strict-overflow`.
- [ ] Wire up or intentionally gate `--with-system-expat`.
- [ ] Wire up or intentionally gate `--with-system-libmpdec`.
- [ ] Wire up or intentionally gate `--with-thread-sanitizer`.
- [ ] Wire up or intentionally gate `--with-trace-refs`.
- [ ] Wire up or intentionally gate `--with-undefined-behavior-sanitizer`.
- [ ] Wire up or intentionally gate `--with-valgrind`.
- [ ] Wire up or intentionally gate `--with-wheel-pkg-dir`.
- [ ] Fix toolchain plumbing so configure can locate `GNU ld input path`.
- [ ] Fix toolchain plumbing so configure can locate `x86_64-unknown-linux-gnu-llvm-profdata`.
- [ ] Fix toolchain plumbing so configure can locate `MACHDEP_OBJS`.
- [ ] Fix toolchain plumbing so configure can locate `library containing opendir`.
- [ ] Fix toolchain plumbing so configure can locate `library containing sem_init`.
- [ ] Fix toolchain plumbing so configure can locate `library containing shm_open`.
- [ ] Fix toolchain plumbing so configure can locate `library containing yp_match`.
- [ ] Fix toolchain plumbing so configure can locate `library containing login_tty`.
- [ ] Settle ABI/platform switch for `__fpu_control` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `__fpu_control in -lieee` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `digit size for Python's longs` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `oxide-cc -pthread options needed to detect all undeclared functions` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `x87-style double rounding` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `if --enable-ipv6 is specified` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `if PTHREAD_SCOPE_SYSTEM is supported` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `if sockaddr has sa_len member` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether _XOPEN_SOURCE should be defined` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether byte ordering is bigendian` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether float word ordering is bigendian` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether oxide-c++ also accepts flags for thread support` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether oxide-cc -fno-strict-aliasing accepts and needs -fno-strict-aliasing` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether oxide-cc accepts -Kpthread` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether oxide-cc accepts -Kthread` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether pthreads are available without options` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether to enable large file support` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether wchar_t is usable` inside the CPython config site and libc headers.
- [ ] Settle ABI/platform switch for `whether we can use gcc inline assembler to get and set mc68881 fpcr` inside the CPython config site and libc headers.
- [ ] Document the deliberate platform assumption for `the Android API level` and short-circuit configure to "not Android".
- [ ] Provide a deterministic toolchain path for `GNU ld input path` inside the build scripts.

### Kernel & syscall surface – filesystem, descriptors, and memory (`crates/syscall/syscall/src/{vfs,poll,memory}.rs`, `kernel/src/vfs`, `userspace/libc/src/{unistd,stat}`)
- [ ] Implement VFS I/O handlers and libc wrappers for `close_range`.
- [ ] Implement VFS I/O handlers and libc wrappers for `copy_file_range`.
- [ ] Implement VFS I/O handlers and libc wrappers for `fdatasync`.
- [ ] Implement VFS I/O handlers and libc wrappers for `fseek64`.
- [ ] Implement VFS I/O handlers and libc wrappers for `fstatvfs`.
- [ ] Implement VFS I/O handlers and libc wrappers for `fsync`.
- [ ] Implement VFS I/O handlers and libc wrappers for `ftell64`.
- [ ] Implement VFS I/O handlers and libc wrappers for `ftime`.
- [ ] Implement VFS I/O handlers and libc wrappers for `openat`.
- [ ] Implement VFS I/O handlers and libc wrappers for `pathconf`.
- [ ] Implement VFS I/O handlers and libc wrappers for `pread`.
- [ ] Implement VFS I/O handlers and libc wrappers for `preadv`.
- [ ] Implement VFS I/O handlers and libc wrappers for `preadv2`.
- [ ] Implement VFS I/O handlers and libc wrappers for `pwrite`.
- [ ] Implement VFS I/O handlers and libc wrappers for `pwritev`.
- [ ] Implement VFS I/O handlers and libc wrappers for `pwritev2`.
- [ ] Implement VFS I/O handlers and libc wrappers for `readlink`.
- [ ] Implement VFS I/O handlers and libc wrappers for `readlinkat`.
- [ ] Implement VFS I/O handlers and libc wrappers for `readv`.
- [ ] Implement VFS I/O handlers and libc wrappers for `realpath`.
- [ ] Implement VFS I/O handlers and libc wrappers for `sendfile`.
- [ ] Implement VFS I/O handlers and libc wrappers for `sendfile in -lsendfile`.
- [ ] Implement VFS I/O handlers and libc wrappers for `splice`.
- [ ] Implement VFS I/O handlers and libc wrappers for `statvfs`.
- [ ] Implement VFS I/O handlers and libc wrappers for `sync`.
- [ ] Implement VFS I/O handlers and libc wrappers for `truncate`.
- [ ] Implement VFS I/O handlers and libc wrappers for `writev`.
- [ ] Provide path metadata and directory semantics for `chflags` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `chmod` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `chown` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `faccessat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `fchmod` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `fchmodat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `fchown` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `fchownat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `fdopendir` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `fdwalk` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `fpathconf` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `futimens` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `futimes` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `futimesat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `lchflags` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `lchown` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `link` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `linkat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `lockf` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `lutimes` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `mkdirat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `mkfifo` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `mkfifoat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `mknod` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `mknodat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `plock` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `renameat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `symlink` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `symlinkat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `unlinkat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `utimensat` in kernel VFS and libc.
- [ ] Provide path metadata and directory semantics for `utimes` in kernel VFS and libc.
- [ ] Ensure VFS/libc macros expose `major`.
- [ ] Ensure VFS/libc macros expose `minor`.
- [ ] Ensure VFS/libc macros expose `makedev`.
- [ ] Ensure VFS/libc macros expose `umask`.
- [ ] Implement descriptor/event plumbing for `dup3` in `crates/syscall/syscall/src/poll.rs` and libc.
- [ ] Implement descriptor/event plumbing for `epoll_create` in `crates/syscall/syscall/src/poll.rs` and libc.
- [ ] Implement descriptor/event plumbing for `epoll_create1` in `crates/syscall/syscall/src/poll.rs` and libc.
- [ ] Implement descriptor/event plumbing for `eventfd` in `crates/syscall/syscall/src/poll.rs` and libc.
- [ ] Implement descriptor/event plumbing for `kqueue` in `crates/syscall/syscall/src/poll.rs` and libc.
- [ ] Implement descriptor/event plumbing for `pipe2` in `crates/syscall/syscall/src/poll.rs` and libc.
- [ ] Implement descriptor/event plumbing for `poll` in `crates/syscall/syscall/src/poll.rs` and libc.
- [ ] Implement descriptor/event plumbing for `socketpair` in `crates/syscall/syscall/src/poll.rs` and libc.
- [ ] Wire memory management support for `madvise` across kernel and libc.
- [ ] Wire memory management support for `memfd_create` across kernel and libc.
- [ ] Wire memory management support for `mremap` across kernel and libc.
- [ ] Wire memory management support for `posix_fadvise` across kernel and libc.
- [ ] Wire memory management support for `posix_fallocate` across kernel and libc.
- [ ] Wire memory management support for `shm_open` across kernel and libc.
- [ ] Wire memory management support for `shm_unlink` across kernel and libc.
- [ ] Wire memory management support for `sem_clockwait` across kernel and libc.
- [ ] Wire memory management support for `sem_getvalue` across kernel and libc.
- [ ] Wire memory management support for `sem_open` across kernel and libc.
- [ ] Wire memory management support for `sem_timedwait` across kernel and libc.
- [ ] Wire memory management support for `sem_unlink` across kernel and libc.

### Process lifecycle, scheduling, credentials, namespaces (`crates/proc/proc`, `kernel/src/scheduler.rs`, `userspace/libc/src/process.rs`)
- [ ] Finish process creation/exec support for `execv`.
- [ ] Finish process creation/exec support for `fexecve`.
- [ ] Finish process creation/exec support for `fork`.
- [ ] Finish process creation/exec support for `fork1`.
- [ ] Finish process creation/exec support for `posix_spawn`.
- [ ] Finish process creation/exec support for `posix_spawnp`.
- [ ] Finish process creation/exec support for `rtpSpawn`.
- [ ] Finish process creation/exec support for `setpgrp`.
- [ ] Finish process creation/exec support for `system`.
- [ ] Finish process creation/exec support for `vfork`.
- [ ] Finish process creation/exec support for `wait3`.
- [ ] Finish process creation/exec support for `wait4`.
- [ ] Finish process creation/exec support for `waitid`.
- [ ] Provide scheduler or signal integration for `sched_get_priority_max`.
- [ ] Provide scheduler or signal integration for `sched_rr_get_interval`.
- [ ] Provide scheduler or signal integration for `sched_setaffinity`.
- [ ] Provide scheduler or signal integration for `sched_setparam`.
- [ ] Provide scheduler or signal integration for `sched_setscheduler`.
- [ ] Provide scheduler or signal integration for `pthread_getcpuclockid`.
- [ ] Provide scheduler or signal integration for `pthread_kill`.
- [ ] Expose credentials/limits plumbing for `getgroups` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `getpriority` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `getpwent` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `getresgid` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `getresuid` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `getspent` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `getspnam` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `initgroups` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `prlimit` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `setgroups` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `setpriority` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `setregid` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `setresgid` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `setresuid` and ensure libc bindings.
- [ ] Expose credentials/limits plumbing for `setreuid` and ensure libc bindings.
- [ ] Implement libc readers for `/etc/passwd`.
- [ ] Implement libc readers for `/etc/shadow`.
- [ ] Back kernel counters/syscalls for `getentropy`.
- [ ] Back kernel counters/syscalls for `getloadavg`.
- [ ] Back kernel counters/syscalls for `getrusage`.
- [ ] Back kernel counters/syscalls for `pause`.
- [ ] Back kernel counters/syscalls for `sysconf`.
- [ ] Back kernel counters/syscalls for `times`.
- [ ] Bind namespace primitive `setns` to `crates/container`.
- [ ] Bind namespace primitive `unshare` to `crates/container`.
- [ ] Teach scheduler about `if PTHREAD_SCOPE_SYSTEM is supported` so pthread scope probes succeed.
- [ ] Teach scheduler about `whether pthreads are available without options` so pthread scope probes succeed.

### Signals, timers, and delivery semantics (`crates/signal`, `userspace/libc/src/signal.rs`)
- [ ] Implement pending signal/timer support for `alarm` with libc and librt shims.
- [ ] Implement pending signal/timer support for `clock_nanosleep` with libc and librt shims.
- [ ] Implement pending signal/timer support for `clock_nanosleep in -lrt` with libc and librt shims.
- [ ] Implement pending signal/timer support for `clock_settime` with libc and librt shims.
- [ ] Implement pending signal/timer support for `clock_settime in -lrt` with libc and librt shims.
- [ ] Implement pending signal/timer support for `getitimer` with libc and librt shims.
- [ ] Implement pending signal/timer support for `kill` with libc and librt shims.
- [ ] Implement pending signal/timer support for `killpg` with libc and librt shims.
- [ ] Implement pending signal/timer support for `setitimer` with libc and librt shims.
- [ ] Implement pending signal/timer support for `sigaction` with libc and librt shims.
- [ ] Implement pending signal/timer support for `sigaltstack` with libc and librt shims.
- [ ] Implement pending signal/timer support for `siginterrupt` with libc and librt shims.
- [ ] Implement pending signal/timer support for `sigpending` with libc and librt shims.
- [ ] Implement pending signal/timer support for `sigrelse` with libc and librt shims.
- [ ] Implement pending signal/timer support for `sigtimedwait` with libc and librt shims.
- [ ] Implement pending signal/timer support for `sigwait` with libc and librt shims.
- [ ] Implement pending signal/timer support for `sigwaitinfo` with libc and librt shims.
- [ ] Implement pending signal/timer support for `timegm` with libc and librt shims.
- [ ] Implement pending signal/timer support for `wcsftime` with libc and librt shims.
- [ ] Implement pending signal/timer support for `working tzset()` with libc and librt shims.
- [ ] Sync pthread signal integration for `pthread_getcpuclockid`.
- [ ] Sync pthread signal integration for `pthread_kill`.

### Networking, hostname plumbing, and IPv6 (`crates/syscall/syscall/src/socket.rs`, `userspace/libc/src/socket.rs`, `userspace/networkd`)
- [ ] Cover socket primitive `CAN_RAW_FD_FRAMES` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `CAN_RAW_JOIN_FILTERS` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `accept` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `accept4` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `getaddrinfo` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `gethostbyname_r` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `getnameinfo` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `getpeername` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `getsockname` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `if_nameindex` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `inet_aton` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `inet_aton in -lc` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `inet_aton in -lresolv` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `inet_pton` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `libnsl` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `sockaddr_alg` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `socket in -lsocket` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `socklen_t` and hook it into networking/userspace resolvers.
- [ ] Cover socket primitive `t_open in -lnsl` and hook it into networking/userspace resolvers.
- [ ] Satisfy the configure check `if --enable-ipv6 is specified` by fleshing out IPv6 data structures.
- [ ] Satisfy the configure check `if sockaddr has sa_len member` by fleshing out IPv6 data structures.
- [ ] Implement hostname/domain plumbing for `gethostname` with persistent `/etc/hostname` updates.
- [ ] Implement hostname/domain plumbing for `sethostname` with persistent `/etc/hostname` updates.

### PTY, terminals, and login integration (`kernel/src/tty`, `userspace/libc/src/tty.rs`, `userspace/login`, `userspace/getty`)
- [ ] Provide PTY or TTY helper `/dev/ptc` across kernel and libc.
- [ ] Provide PTY or TTY helper `/dev/ptmx` across kernel and libc.
- [ ] Provide PTY or TTY helper `_getpty` across kernel and libc.
- [ ] Provide PTY or TTY helper `ctermid` across kernel and libc.
- [ ] Provide PTY or TTY helper `ctermid_r` across kernel and libc.
- [ ] Provide PTY or TTY helper `forkpty` across kernel and libc.
- [ ] Provide PTY or TTY helper `forkpty in -lbsd` across kernel and libc.
- [ ] Provide PTY or TTY helper `forkpty in -lutil` across kernel and libc.
- [ ] Provide PTY or TTY helper `openpty` across kernel and libc.
- [ ] Provide PTY or TTY helper `openpty in -lbsd` across kernel and libc.
- [ ] Provide PTY or TTY helper `openpty in -lutil` across kernel and libc.
- [ ] Provide PTY or TTY helper `ttyname` across kernel and libc.
- [ ] Ensure `userspace/libutil` exposes `login_tty`.
- [ ] Extend getty/pty tests for the configure probe `library containing login_tty`.

### Userspace libc headers & struct layout (`userspace/libc/include`, `kernel/include`) 
- [ ] Provide libc header `alloca.h` so configure can locate it.
- [ ] Provide libc header `asm/types.h` so configure can locate it.
- [ ] Provide libc header `bluetooth.h` so configure can locate it.
- [ ] Provide libc header `bluetooth/bluetooth.h` so configure can locate it.
- [ ] Provide libc header `conio.h` so configure can locate it.
- [ ] Provide libc header `crypt.h` so configure can locate it.
- [ ] Provide libc header `curses.h` so configure can locate it.
- [ ] Provide libc header `db.h` so configure can locate it.
- [ ] Provide libc header `direct.h` so configure can locate it.
- [ ] Provide libc header `gdbm-ndbm.h` so configure can locate it.
- [ ] Provide libc header `gdbm.h` so configure can locate it.
- [ ] Provide libc header `gdbm/ndbm.h` so configure can locate it.
- [ ] Provide libc header `ieeefp.h` so configure can locate it.
- [ ] Provide libc header `io.h` so configure can locate it.
- [ ] Provide libc header `langinfo.h` so configure can locate it.
- [ ] Provide libc header `libintl.h` so configure can locate it.
- [ ] Provide libc header `libutil.h` so configure can locate it.
- [ ] Provide libc header `linux/auxvec.h` so configure can locate it.
- [ ] Provide libc header `linux/can.h` so configure can locate it.
- [ ] Provide libc header `linux/can/bcm.h` so configure can locate it.
- [ ] Provide libc header `linux/can/j1939.h` so configure can locate it.
- [ ] Provide libc header `linux/can/raw.h` so configure can locate it.
- [ ] Provide libc header `linux/fs.h` so configure can locate it.
- [ ] Provide libc header `linux/limits.h` so configure can locate it.
- [ ] Provide libc header `linux/memfd.h` so configure can locate it.
- [ ] Provide libc header `linux/netlink.h` so configure can locate it.
- [ ] Provide libc header `linux/qrtr.h` so configure can locate it.
- [ ] Provide libc header `linux/soundcard.h` so configure can locate it.
- [ ] Provide libc header `linux/tipc.h` so configure can locate it.
- [ ] Provide libc header `linux/vm_sockets.h` so configure can locate it.
- [ ] Provide libc header `linux/wait.h` so configure can locate it.
- [ ] Provide libc header `minix/config.h` so configure can locate it.
- [ ] Provide libc header `ncurses.h` so configure can locate it.
- [ ] Provide libc header `ndbm.h` so configure can locate it.
- [ ] Provide libc header `net/ethernet.h` so configure can locate it.
- [ ] Provide libc header `net/if.h` so configure can locate it.
- [ ] Provide libc header `netcan/can.h` so configure can locate it.
- [ ] Provide libc header `netdb.h` so configure can locate it.
- [ ] Provide libc header `netinet/in.h` so configure can locate it.
- [ ] Provide libc header `netpacket/packet.h` so configure can locate it.
- [ ] Provide libc header `panel.h` so configure can locate it.
- [ ] Provide libc header `poll.h` so configure can locate it.
- [ ] Provide libc header `process.h` so configure can locate it.
- [ ] Provide libc header `pty.h` so configure can locate it.
- [ ] Provide libc header `readline/readline.h` so configure can locate it.
- [ ] Provide libc header `sched.h` so configure can locate it.
- [ ] Provide libc header `shadow.h` so configure can locate it.
- [ ] Provide libc header `spawn.h` so configure can locate it.
- [ ] Provide libc header `sqlite3.h` so configure can locate it.
- [ ] Provide libc header `stdatomic.h` so configure can locate it.
- [ ] Provide libc header `strings.h` so configure can locate it.
- [ ] Provide libc header `stropts.h` so configure can locate it.
- [ ] Provide libc header `sys/audioio.h` so configure can locate it.
- [ ] Provide libc header `sys/auxv.h` so configure can locate it.
- [ ] Provide libc header `sys/bsdtty.h` so configure can locate it.
- [ ] Provide libc header `sys/devpoll.h` so configure can locate it.
- [ ] Provide libc header `sys/endian.h` so configure can locate it.
- [ ] Provide libc header `sys/epoll.h` so configure can locate it.
- [ ] Provide libc header `sys/event.h` so configure can locate it.
- [ ] Provide libc header `sys/eventfd.h` so configure can locate it.
- [ ] Provide libc header `sys/file.h` so configure can locate it.
- [ ] Provide libc header `sys/kern_control.h` so configure can locate it.
- [ ] Provide libc header `sys/loadavg.h` so configure can locate it.
- [ ] Provide libc header `sys/lock.h` so configure can locate it.
- [ ] Provide libc header `sys/memfd.h` so configure can locate it.
- [ ] Provide libc header `sys/mkdev.h` so configure can locate it.
- [ ] Provide libc header `sys/modem.h` so configure can locate it.
- [ ] Provide libc header `sys/param.h` so configure can locate it.
- [ ] Provide libc header `sys/poll.h` so configure can locate it.
- [ ] Provide libc header `sys/resource.h` so configure can locate it.
- [ ] Provide libc header `sys/sendfile.h` so configure can locate it.
- [ ] Provide libc header `sys/socket.h` so configure can locate it.
- [ ] Provide libc header `sys/soundcard.h` so configure can locate it.
- [ ] Provide libc header `sys/statvfs.h` so configure can locate it.
- [ ] Provide libc header `sys/sys_domain.h` so configure can locate it.
- [ ] Provide libc header `sys/sysmacros.h` so configure can locate it.
- [ ] Provide libc header `sys/termio.h` so configure can locate it.
- [ ] Provide libc header `sys/uio.h` so configure can locate it.
- [ ] Provide libc header `sys/un.h` so configure can locate it.
- [ ] Provide libc header `sys/xattr.h` so configure can locate it.
- [ ] Provide libc header `sysexits.h` so configure can locate it.
- [ ] Provide libc header `term.h` so configure can locate it.
- [ ] Provide libc header `time.h that defines altzone` so configure can locate it.
- [ ] Provide libc header `util.h` so configure can locate it.
- [ ] Provide libc header `utmp.h` so configure can locate it.
- [ ] Provide libc header `uuid.h` so configure can locate it.
- [ ] Provide libc header `uuid/uuid.h` so configure can locate it.
- [ ] Align struct/layout probe `struct stat.st_birthtime` with the kernel ABI.
- [ ] Align struct/layout probe `struct stat.st_flags` with the kernel ABI.
- [ ] Align struct/layout probe `struct stat.st_gen` with the kernel ABI.
- [ ] Align struct/layout probe `tv_nsec2 in struct stat` with the kernel ABI.
- [ ] Align struct/layout probe `whether RTLD_MEMBER is declared` with the kernel ABI.
- [ ] Align struct/layout probe `whether WINDOW has _flags` with the kernel ABI.
- [ ] Align struct/layout probe `whether mvwdelch is an expression` with the kernel ABI.
- [ ] Align struct/layout probe `whether compiling and linking against OpenSSL works` with the kernel ABI.
- [ ] Align struct/layout probe `whether OpenSSL provides required hashlib module APIs` with the kernel ABI.
- [ ] Align struct/layout probe `whether OpenSSL provides required ssl module APIs` with the kernel ABI.

### Libc runtime, intl/locale, crypto, and loader (`userspace/libc/src/{stdio,stdlib,locale,crypto}`, `userspace/ld`, `userspace/libdl`, `external/libffi`)
- [ ] Finish libc runtime support for `case-insensitive build directory`.
- [ ] Finish libc runtime support for `confstr`.
- [ ] Finish libc runtime support for `explicit_bzero`.
- [ ] Finish libc runtime support for `explicit_memset`.
- [ ] Finish libc runtime support for `getc_unlocked() and friends`.
- [ ] Finish libc runtime support for `getlogin`.
- [ ] Finish libc runtime support for `getwd`.
- [ ] Finish libc runtime support for `tempnam`.
- [ ] Finish libc runtime support for `tmpnam_r`.
- [ ] Finish libc runtime support for `bind_textdomain_codeset`.
- [ ] Finish libc runtime support for `textdomain in -lintl`.
- [ ] Integrate crypto/UUID support for `crypt or crypt_r` with libc and trust stores.
- [ ] Integrate crypto/UUID support for `libb2` with libc and trust stores.
- [ ] Integrate crypto/UUID support for `uuid >= 2.20` with libc and trust stores.
- [ ] Ensure OpenSSL support for `whether compiling and linking against OpenSSL works` passes configure.
- [ ] Ensure OpenSSL support for `whether OpenSSL provides required hashlib module APIs` passes configure.
- [ ] Ensure OpenSSL support for `whether OpenSSL provides required ssl module APIs` passes configure.
- [ ] Update loader/libdl/ffi handling for `_dyld_shared_cache_contains_path`.
- [ ] Update loader/libdl/ffi handling for `dlopen`.
- [ ] Update loader/libdl/ffi handling for `dlopen in -ldl`.
- [ ] Update loader/libdl/ffi handling for `dyld`.
- [ ] Update loader/libdl/ffi handling for `shl_load in -ldld`.
- [ ] Update loader/libdl/ffi handling for `ffi_closure_alloc`.
- [ ] Update loader/libdl/ffi handling for `ffi_prep_cif_var`.
- [ ] Update loader/libdl/ffi handling for `ffi_prep_closure_loc`.

### Testing and diagnostics (`userspace/syscall-tests`, `tools/ci`, `external/cpython/Lib/test`)
- [ ] Add regression test coverage for `broken mbstowcs`.
- [ ] Add regression test coverage for `broken nice()`.
- [ ] Add regression test coverage for `broken poll()`.
- [ ] Add regression test coverage for `broken unsetenv`.
- [ ] Extend syscall/integration suites so each kernel function listed earlier has a libc test (userspace/syscall-tests + Python test suite) before shipping.
