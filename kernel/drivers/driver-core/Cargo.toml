[package]
name = "driver-core"
version = "0.1.0"
edition = "2021"

[dependencies]
pci = { path = "../pci" }
os_log = { workspace = true }
spin = { workspace = true }

[lib]
name = "driver_core"
path = "src/lib.rs"
