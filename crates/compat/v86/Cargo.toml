[package]
name = "v86"
version = "0.1.0"
edition = "2021"
authors = ["OXIDE Contributors"]
description = "Virtual 8086 mode DOS emulation for OXIDE OS"
license = "MIT OR Apache-2.0"

[dependencies]
spin = "0.9"
bitflags = "2.4"

[features]
default = []
std = []
