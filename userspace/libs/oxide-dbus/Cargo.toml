[package]
name = "oxide-dbus"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Custom D-Bus wire protocol library for OXIDE OS — libdbus-1 compatible C API"

[dependencies]

[lib]
crate-type = ["rlib"]
