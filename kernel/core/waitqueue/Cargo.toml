[package]
name = "waitqueue"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Generic wait queues for OXIDE OS — fixed-capacity, lock-free, zero-alloc"

[dependencies]
