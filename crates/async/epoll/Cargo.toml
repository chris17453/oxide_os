[package]
name = "epoll"
version = "0.1.0"
edition = "2021"
authors = ["OXIDE Contributors"]
description = "epoll event notification for OXIDE OS"
license = "MIT OR Apache-2.0"

[dependencies]
spin = "0.9"

[features]
default = []
std = []
