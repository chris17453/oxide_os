[package]
name = "efflux-vmm"
version = "0.1.0"
edition = "2024"
description = "Virtual Machine Monitor for Efflux OS"
license = "MIT OR Apache-2.0"

[dependencies]
spin = "0.9"

[features]
default = []
