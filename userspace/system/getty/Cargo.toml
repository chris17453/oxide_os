[package]
name = "getty"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Getty terminal manager for OXIDE OS"

[dependencies]
libc = { path = "../../libs/libc" }
